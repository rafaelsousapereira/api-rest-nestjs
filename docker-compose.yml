version: '3.9'

services:
  postgres:
    container_name: api_rest_nestjs
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: api_nestjs
  adminer:
    image: adminer
    ports:
      - 8080:8080